from pathlib import Path


async def transcribe(audio_path: Path) -> str:
    """Transcribe an audio file to text. Implementation TBD."""
    raise NotImplementedError("Transcription API not yet configured")
